import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const actions=readFileSync("features/settings/actions.ts","utf8"),profiles=readFileSync("supabase/migrations/202609010001_create_profiles.sql","utf8"),finance=readFileSync("supabase/migrations/202609010006_create_finance.sql","utf8");describe("settings security contract",()=>{it("keeps account writes owner-scoped and profile rows unique",()=>{expect(actions).toContain('.eq("user_id",user.id)');expect(profiles).toContain("user_id uuid primary key");expect(profiles).toContain("profiles_update_own")});it("uses optimistic concurrency rather than silently overwriting",()=>expect(actions.match(/\.eq\("updated_at",parsed\.data\.updatedAt\)/g)?.length).toBe(2));it("checks all financial records and retains the database lock",()=>{for(const table of["cost_items","payments","actual_expenses","financial_adjustments"])expect(actions).toContain(`"${table}"`);expect(finance).toContain("base_currency_locked_by_financial_data")});it("never accepts a user id from form data",()=>expect(actions).not.toMatch(/data\.get\("user/))});
