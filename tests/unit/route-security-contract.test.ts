import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/202609010004_create_route.sql","utf8");describe("route database contract",()=>{it("enables owner-derived RLS for both tables",()=>{expect(sql).toContain("alter table public.stops enable row level security");expect(sql).toContain("alter table public.travel_legs enable row level security");expect(sql.match(/auth\.uid\(\)/g)?.length).toBeGreaterThanOrEqual(5)});it("uses atomic database functions for reorder and delete",()=>{expect(sql).toContain("function public.reorder_route_stops");expect(sql).toContain("function public.delete_route_stop");expect(sql).toContain("set constraints stops_trip_position_unique deferred")});it("preserves affected legs for review on reorder",()=>expect(sql).toMatch(/update public\.travel_legs leg set review_required = true/));it("uses integer minor units and same-trip endpoint foreign keys",()=>{expect(sql).toContain("price_minor bigint");expect(sql).toContain("foreign key (from_stop_id, trip_id)");expect(sql).toContain("foreign key (to_stop_id, trip_id)")})});
