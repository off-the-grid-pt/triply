import type { ChecklistItem } from "./types";export type DueState="completed"|"overdue"|"due_today"|"upcoming"|"none";export function deriveDueState(item:ChecklistItem,today:string):DueState{if(item.isCompleted)return"completed";if(!item.dueDate)return"none";if(item.dueDate<today)return"overdue";if(item.dueDate===today)return"due_today";return"upcoming";}export function safeBookingUrl(value:string):string|null{try{const url=new URL(value);return url.protocol==="http:"||url.protocol==="https:"?url.toString():null;}catch{return null;}}
