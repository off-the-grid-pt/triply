import Link from "next/link"; import { notFound } from "next/navigation"; import { AttachmentForm,DeleteDocumentForm,DocumentForm,RemoveAttachmentForm } from "@/features/documents/components/forms"; import { getOwnedDocument } from "@/features/documents/queries"; import { getOwnedPlanning } from "@/features/planning/queries"; import { getOwnedRoute } from "@/features/route/queries";
export default async function EditDocumentPage({params}:{params:Promise<{tripId:string;documentId:string}>}){const{tripId,documentId}=await params;const[document,route,planning]=await Promise.all([getOwnedDocument(tripId,documentId),getOwnedRoute(tripId),getOwnedPlanning(tripId)]);if(!document||!route||!planning)notFound();return <main className="min-h-screen bg-background px-5 py-8"><div className="mx-auto max-w-2xl"><Link href={`/trips/${tripId}/documents`}>← Voltar aos documentos</Link><h1 className="my-8 text-4xl font-semibold">Editar documento</h1><section className="rounded-feature border border-border bg-card p-6 sm:p-8"><DocumentForm tripId={tripId} document={document} stops={route.stops} legs={route.legs} reservations={planning.reservations}/></section><section className="mt-6 rounded-feature border border-border bg-card p-6"><h2 className="text-xl font-semibold">Ficheiro privado</h2>{document.attachmentPath?<p className="my-3 break-all text-sm">Atual: {document.attachmentName} · <a className="font-semibold underline" href={`/trips/${tripId}/documents/${document.id}/download`}>Abrir / descarregar</a></p>:<p className="my-3 text-sm text-muted-foreground">Ainda não existe ficheiro associado.</p>}<AttachmentForm tripId={tripId} document={document}/>{document.attachmentPath?<div className="mt-6 border-t pt-5"><RemoveAttachmentForm tripId={tripId} documentId={document.id}/></div>:null}</section><section className="mt-6 rounded-feature border border-destructive bg-card p-6"><h2 className="text-xl font-semibold text-destructive">Zona destrutiva</h2><div className="mt-4"><DeleteDocumentForm tripId={tripId} document={document}/></div></section></div></main>}
