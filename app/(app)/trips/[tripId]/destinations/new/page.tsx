import { randomUUID } from "node:crypto";import Link from "next/link";import { notFound } from "next/navigation";import { StopForm } from "@/features/route/components/stop-form";import { getOwnedTrip } from "@/features/trips/queries";import { tripIdSchema } from "@/features/trips/schemas";
export default async function NewDestination({params}:PageProps<"/trips/[tripId]/destinations/new">){const{tripId}=await params;if(!tripIdSchema.safeParse(tripId).success)notFound();const trip=await getOwnedTrip(tripId);if(!trip)notFound();return <main className="min-h-screen bg-background px-5 py-8"><div className="mx-auto max-w-3xl"><Link href={`/trips/${tripId}`} className="text-sm">← Voltar à viagem</Link><header className="my-8"><p className="text-xs font-semibold uppercase tracking-[.18em] text-muted-foreground">Novo destino</p><h1 className="mt-3 text-4xl font-semibold">Adicionar Stop</h1><p className="mt-3 text-muted-foreground">Datas da viagem: {trip.startDate} — {trip.endDate}. Destinos repetidos são permitidos.</p></header><section className="rounded-feature border bg-card p-6 sm:p-8"><StopForm tripId={tripId} initial={{placeName:"",countryCode:"",countryName:"",arrivalDate:trip.startDate,departureDate:trip.endDate,timezone:"",notes:"",createRequestId:randomUUID()}}/></section></div></main>}
