import Link from "next/link";import { notFound } from "next/navigation";import { CategoryManager } from "@/features/finance/components/category-manager";import { FinanceDashboard } from "@/features/finance/components/dashboard";import { getOwnedFinance } from "@/features/finance/queries";import { getOwnedRoute } from "@/features/route/queries";import { getOwnedTrip } from "@/features/trips/queries";
export default async function FinancePage({params}:PageProps<"/trips/[tripId]/finance">){const{tripId}=await params,[trip,data,route]=await Promise.all([getOwnedTrip(tripId),getOwnedFinance(tripId),getOwnedRoute(tripId)]);if(!trip||!data||!route)notFound();return <main className="min-h-screen bg-background px-5 py-8 text-foreground"><div className="mx-auto max-w-5xl"><Link href={`/trips/${tripId}`} className="text-sm font-medium text-muted-foreground">← Voltar à viagem</Link><header className="my-8"><p className="text-xs font-semibold uppercase tracking-[.18em] text-muted-foreground">Orçamento e despesas</p><h1 className="mt-2 text-4xl font-semibold">{trip.name}</h1><p className="mt-3 text-muted-foreground">Valores consolidados em {trip.baseCurrency}. Estimado, comprometido, pago e real permanecem distintos.</p></header><FinanceDashboard trip={trip} data={data} route={route}/><CategoryManager tripId={tripId} categories={data.categories}/></div></main>}
